//using BubbleShop.Application.Common.Models;
//using BubbleShop.Domain.Enums;
//using MediatR;

//namespace BubbleShop.Application.Features.Deliveries.Queries.GetDeliveryStatus;

//public sealed record DeliveryStatusDto(Guid OrderId, DeliveryStatus Status, string? TrackingNumber);
//public sealed record GetDeliveryStatusQuery(Guid OrderId) : IRequest<Result<DeliveryStatusDto>>;
